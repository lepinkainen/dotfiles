#!/bin/bash
# Re-sync fish plugins whenever the managed fish_plugins file changes.
# fish_plugins hash: {{ include "dot_config/private_fish/fish_plugins" | sha256sum }}
set -euo pipefail

command -v fish >/dev/null 2>&1 || exit 0
fish -c "functions -q fisher" || exit 0

exec fish -c "fisher update"
